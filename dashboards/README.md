# Dashboard skeletons

This area groups dashboard and workbook skeletons for implementation labs.

Use subfolders for the visualization surface:

- `azure-monitor/` for Azure Monitor workbook and query skeletons.
- `grafana/` for Grafana dashboard skeletons.

Do not commit customer telemetry exports, screenshots, workspace IDs, or live
query results.
