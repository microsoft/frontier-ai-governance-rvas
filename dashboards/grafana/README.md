# Grafana dashboard skeletons

Use this folder for future Grafana dashboard skeletons that visualize
OpenTelemetry, runtime, evaluation, cost, or control-plane signals.

Each asset should document:

- data source assumptions;
- required labels or correlation fields;
- non-production validation path;
- dashboard owner;
- evidence reference and review cadence.
