# Live-only module

This module retains no implementation artifacts. Microsoft Agent 365 holds the inventory record,
and the runtime owner keeps the A2A definition, agent card, and integration source.

Do not add customer-specific records, exports, screenshots, endpoints, credentials, or copied
agent metadata here.
