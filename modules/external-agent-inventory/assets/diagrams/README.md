# Module diagrams

Keep one editable source and its rendered SVG for each diagram. Use either `.drawio` or
`.excalidraw`, never both for the same SVG. `external-agent-inventory-architecture.drawio` shows
the supported onboarding routes into Agent Registry.

Use descriptive alt text and keep official Microsoft icons unchanged.
