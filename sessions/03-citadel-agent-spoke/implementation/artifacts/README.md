# Agent Spoke artifacts

The Citadel release and spoke profile pin the workload landing-zone source and record the selected deployment shape. The `agents/policy-assistant` files define the bounded agent deployed after the spoke is ready.

Keep environment secrets and generated endpoints outside this repository.
