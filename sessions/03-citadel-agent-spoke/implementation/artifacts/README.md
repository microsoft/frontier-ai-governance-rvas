# Agent Spoke artifacts

`citadel/release.json` pins the AI Landing Zones architecture and Bicep implementation.
`citadel/spoke-profile.json` maps the approved workload boundary to the upstream Bicep parameters.
The `agents/policy-assistant` files define the bounded agent deployed after the landing zone is
ready.

Keep credentials, generated endpoints, and deployment output outside this repository.
