# Session diagrams

`red-team-defense-loop.excalidraw` is the authoritative editable source. Its generated
`red-team-defense-loop.svg` is the published version used by the guide and deck.

The diagram keeps Defender-to-SOC delivery separate from the red-team comparison. Route delivery
joins the operations handoff, but it does not prove that remediation improved the per-key result.

Use the bundled Microsoft service icons without recoloring, cropping, rotating, or
distorting them. Keep product names visible near their icons.
