# Implementation artifacts

`agent-deployment.json` records one group-scoped Agent Registry deployment. Microsoft 365 admin
center remains authoritative for installation, permission consent, and user availability.

Complete every `__REQUIRED_*__` value before running preflight. Keep tenant IDs, user identities,
and permission-consent records in the approved customer configuration system.
