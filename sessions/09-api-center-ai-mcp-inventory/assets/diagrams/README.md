# Session diagrams

`api-center-inventory-flow.excalidraw` is the editable source for the session inventory visual.
Export it to `api-center-inventory-flow.svg` after every change. The SVG is the deck-ready copy.

Use the bundled Microsoft service icons without recoloring, cropping, rotating, or
distorting them. Keep product names visible near their icons.
