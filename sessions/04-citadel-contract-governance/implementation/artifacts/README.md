# Contract governance artifacts

The three parameter files are customer-owned overlays for the pinned Citadel backend, access, and publish contract modules. The governance records define the tool boundary and operating decisions. The KQL query supports payload-free MCP traffic review.

The publish contract is a preview feature. Keep it disabled until the service owner accepts that boundary.
