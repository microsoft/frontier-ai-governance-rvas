# Session 01 diagrams

`session-flow.excalidraw` is the authoritative editable source. It traces the customer-owned Bicep
deployment through the Foundry baseline and its workspace-based Application Insights connection.
The flow ends with operations taking ownership of inventory and migration work.

`session-flow.svg` is the exported briefing asset referenced by `deck.md`. Regenerate it after
changing the Excalidraw source.

The diagram uses the approved Excalidraw palette. Its scope remains sandbox or nonproduction.
