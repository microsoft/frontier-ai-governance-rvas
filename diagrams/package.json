{
  "name": "rvas-diagrams",
  "private": true,
  "type": "module",
  "description": "Excalidraw sources + builders for the RVAS docs diagrams.",
  "scripts": {
    "build": "node build-all.mjs",
    "render": "node render-all.mjs",
    "publish-svg": "cp *.svg ../docs/assets/diagrams/",
    "all": "npm run build && npm run render && npm run publish-svg"
  },
  "devDependencies": {
    "playwright": "^1.0.0"
  }
}
